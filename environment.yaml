name: dlnlp-final-uceezl8
channels:
  - defaults
dependencies:
  - python=3.9
  - black                               # formatter
  - scikit-learn                        # split dataset
  - pandas                              # load dataframe
  - ipython                             # debugging
  - matplotlib                          # draw figures
  - numpy
  - pip
  - pip:
    - -r requirements.txt
